# Epic 00 — Fundação · Plano de Implementação

> **Para agentes:** SUB-SKILL OBRIGATÓRIA: use `superpowers:subagent-driven-development` (recomendado) ou `superpowers:executing-plans` para implementar task a task. Os passos usam checkbox (`- [ ]`).

**Objetivo:** deixar o projeto pronto para receber features — scaffold, ferramental, tema, casca da aplicação e as três bibliotecas compartilhadas (`api-errors`, `query-client`, `messages`) — com CI verde no PR.

**Arquitetura:** Next.js App Router com `src/`, que mais adiante também será o BFF. Nesta fundação não existe nenhuma chamada à API: entregamos apenas a infraestrutura, os utilitários puros que os epics seguintes consomem e a casca visual. O tema vive num cookie lido no servidor, então o HTML já chega com o tema certo.

**Stack:** Next.js (App Router), TypeScript, Tailwind CSS, shadcn/ui, TanStack Query, Vitest, Testing Library, MSW, GitHub Actions.

**Spec:** [`../specs/000-fundacao-arquitetura.md`](../specs/000-fundacao-arquitetura.md) · **Backlog:** [`../BACKLOG.md`](../BACKLOG.md) (00.1–00.11) · **Branch:** `feat/00-fundacao`

## Restrições globais

- **Porta 3001** em dev e start. A `:3000` é do backend.
- **`API_URL` é server-only.** Nenhuma variável com prefixo `NEXT_PUBLIC_` neste epic. A URL da API não pode entrar no bundle do cliente.
- **Código em inglês** (arquivos, variáveis, funções, tipos). **Textos visíveis em português.**
- **Nenhum arquivo acima de ~200 linhas.** Passou disso, tem responsabilidade demais.
- **Conventional commits**, atômicos, corpo em português explicando o porquê.
- **Nada direto em `main`.** Todo o epic vive em `feat/00-fundacao` e entra por PR.
- Sem gradiente roxo-azul, sem emoji como ícone, sem sombra flutuante. Paleta sóbria, uma cor de ação primária.

---

### Task 1: Scaffold do projeto (backlog 00.1)

**Arquivos:**
- Criar: raiz do projeto (`package.json`, `tsconfig.json`, `next.config.ts`, `src/app/`)
- Modificar: `package.json` (scripts)

**Interfaces:**
- Consome: nada
- Produz: alias `@/*` apontando para `src/*`; scripts `dev`, `build`, `start`

- [ ] **Passo 1: Gerar o projeto na pasta atual**

O repositório já tem `CLAUDE.md`, `docs/` e `.gitignore` — o scaffold precisa conviver com eles.

```bash
npx create-next-app@latest . \
  --typescript --tailwind --eslint --app --src-dir \
  --import-alias "@/*" --use-npm --yes
```

Se o CLI reclamar que o diretório não está vazio, responda que sim para prosseguir; ele preserva arquivos que não colidem. Confira depois que `CLAUDE.md`, `docs/` e `.gitignore` continuam lá (`git status`).

- [ ] **Passo 2: Fixar a porta 3001 nos scripts**

Em `package.json`, os scripts `dev` e `start`:

```json
{
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start -p 3001"
  }
}
```

- [ ] **Passo 3: Verificar que sobe**

Run: `npm run dev`
Esperado: servidor em `http://localhost:3001`, página inicial carrega. Encerre com `Ctrl+C`.

- [ ] **Passo 4: Verificar que compila**

Run: `npm run build`
Esperado: build conclui sem erro.

- [ ] **Passo 5: Commit**

```bash
git add -A
git commit -m "chore: scaffold do Next.js com App Router, TypeScript e Tailwind

Projeto gerado na porta 3001 porque a 3000 é do backend."
```

---

### Task 2: Lint, formatação e typecheck (backlog 00.2)

**Arquivos:**
- Criar: `.prettierrc`, `.prettierignore`
- Modificar: `eslint.config.mjs`, `package.json`

**Interfaces:**
- Consome: scaffold da Task 1
- Produz: scripts `lint`, `format`, `format:check`, `typecheck` — usados pelo CI na Task 11

- [ ] **Passo 1: Instalar Prettier e o plugin de ordenação de imports**

```bash
npm i -D prettier eslint-config-prettier @ianvs/prettier-plugin-sort-imports
```

- [ ] **Passo 2: Configurar o Prettier**

`.prettierrc`:

```json
{
  "semi": true,
  "singleQuote": false,
  "trailingComma": "all",
  "printWidth": 100,
  "plugins": ["@ianvs/prettier-plugin-sort-imports"],
  "importOrder": [
    "^(react|next)(.*)$",
    "<THIRD_PARTY_MODULES>",
    "^@/(server|lib|types)/(.*)$",
    "^@/(components|features|stores)/(.*)$",
    "^[./]"
  ],
  "importOrderSeparation": true
}
```

`.prettierignore`:

```
.next
node_modules
coverage
package-lock.json
docs/doc-frontend
```

- [ ] **Passo 3: Desligar as regras de estilo do ESLint**

Em `eslint.config.mjs`, acrescente `"prettier"` ao final da lista de configs estendidas, para o ESLint não brigar com o Prettier sobre formatação.

- [ ] **Passo 4: Adicionar os scripts**

```json
{
  "scripts": {
    "lint": "next lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit"
  }
}
```

- [ ] **Passo 5: Formatar tudo e verificar**

```bash
npm run format
npm run lint
npm run typecheck
```

Esperado: os três passam sem erro.

- [ ] **Passo 6: Commit**

```bash
git add -A
git commit -m "chore: configura Prettier, ESLint e typecheck

Ordenação de imports por camada (server/lib primeiro, depois UI) para
que o diff de import não vire ruído de revisão."
```

---

### Task 3: Infraestrutura de testes (backlog 00.3)

**Arquivos:**
- Criar: `vitest.config.ts`, `vitest.setup.ts`, `src/test/msw.ts`, `src/test/smoke.test.ts`
- Modificar: `package.json`

**Interfaces:**
- Consome: scaffold da Task 1
- Produz: `npm test`; `server` exportado de `src/test/msw.ts` para as tasks que mockam HTTP

- [ ] **Passo 1: Instalar as dependências de teste**

```bash
npm i -D vitest @vitejs/plugin-react jsdom \
  @testing-library/react @testing-library/dom @testing-library/user-event \
  @testing-library/jest-dom msw
```

- [ ] **Passo 2: Configurar o Vitest**

`vitest.config.ts`:

```ts
import react from "@vitejs/plugin-react";
import { resolve } from "node:path";
import { defineConfig } from "vitest/config";

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: { "@": resolve(__dirname, "./src") },
  },
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: ["./vitest.setup.ts"],
    include: ["src/**/*.test.{ts,tsx}"],
  },
});
```

- [ ] **Passo 3: Criar o setup**

`vitest.setup.ts`:

```ts
import "@testing-library/jest-dom/vitest";

import { cleanup } from "@testing-library/react";
import { afterAll, afterEach, beforeAll } from "vitest";

import { server } from "./src/test/msw";

beforeAll(() => server.listen({ onUnhandledRequest: "error" }));
afterEach(() => {
  server.resetHandlers();
  cleanup();
});
afterAll(() => server.close());
```

`onUnhandledRequest: "error"` é deliberado: requisição não mockada deve quebrar o teste, não passar silenciosamente pela rede.

`src/test/msw.ts`:

```ts
import { setupServer } from "msw/node";

export const server = setupServer();
```

- [ ] **Passo 4: Escrever o teste de fumaça**

`src/test/smoke.test.ts`:

```ts
import { describe, expect, it } from "vitest";

describe("infraestrutura de testes", () => {
  it("roda com jsdom disponível", () => {
    expect(typeof document).toBe("object");
  });
});
```

- [ ] **Passo 5: Adicionar os scripts e rodar**

```json
{
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
```

Run: `npm test`
Esperado: 1 teste passando.

- [ ] **Passo 6: Commit**

```bash
git add -A
git commit -m "test: configura Vitest, Testing Library e MSW

MSW com onUnhandledRequest em 'error': requisição não mockada deve
quebrar o teste em vez de escapar para a rede."
```

---

### Task 4: `ApiError` e parse do erro da API (backlog 00.9)

**Arquivos:**
- Criar: `src/lib/api-errors.ts`, `src/lib/api-errors.test.ts`

**Interfaces:**
- Consome: infraestrutura de testes da Task 3
- Produz:
  - `class ApiError extends Error` com `status: number`, `code: string`, `details?: ApiErrorDetail[]`, `requestId?: string`
  - `interface ApiErrorDetail { path: string; message: string }`
  - `parseApiError(status: number, body: unknown): ApiError`
  - `isApiError(value: unknown): value is ApiError`

Consumido pela Task 5 (retry do Query), pelo `server/api-client.ts` (epic 01) e pelo `useApiFormErrors` (epic 02).

- [ ] **Passo 1: Escrever os testes que falham**

`src/lib/api-errors.test.ts`:

```ts
import { describe, expect, it } from "vitest";

import { ApiError, isApiError, parseApiError } from "./api-errors";

describe("parseApiError", () => {
  it("extrai code, message, details e requestId do formato da API", () => {
    const erro = parseApiError(400, {
      error: {
        code: "VALIDATION_ERROR",
        message: "Dados de entrada inválidos",
        details: [{ path: "email", message: "Invalid email address" }],
        requestId: "4a39b172-b2b3-4df5-a572-a304ce067f62",
      },
    });

    expect(erro.status).toBe(400);
    expect(erro.code).toBe("VALIDATION_ERROR");
    expect(erro.message).toBe("Dados de entrada inválidos");
    expect(erro.details).toEqual([{ path: "email", message: "Invalid email address" }]);
    expect(erro.requestId).toBe("4a39b172-b2b3-4df5-a572-a304ce067f62");
  });

  it("usa fallback quando o corpo não segue o formato esperado", () => {
    const erro = parseApiError(502, "<html>Bad Gateway</html>");

    expect(erro.status).toBe(502);
    expect(erro.code).toBe("UNKNOWN");
    expect(erro.message).toBe("Falha inesperada ao falar com o servidor");
    expect(erro.details).toBeUndefined();
  });

  it("não inventa details quando o campo não é uma lista", () => {
    const erro = parseApiError(400, { error: { code: "X", message: "m", details: "nope" } });

    expect(erro.details).toBeUndefined();
  });
});

describe("isApiError", () => {
  it("reconhece um ApiError", () => {
    expect(isApiError(new ApiError(404, "NOT_FOUND", "não achei"))).toBe(true);
  });

  it("rejeita um Error comum", () => {
    expect(isApiError(new Error("qualquer"))).toBe(false);
  });
});
```

- [ ] **Passo 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/api-errors.test.ts`
Esperado: FALHA — não consegue resolver `./api-errors`.

- [ ] **Passo 3: Implementar**

`src/lib/api-errors.ts`:

```ts
export interface ApiErrorDetail {
  path: string;
  message: string;
}

/** Erro no formato único da API: { error: { code, message, details?, requestId } }. */
export class ApiError extends Error {
  constructor(
    readonly status: number,
    readonly code: string,
    message: string,
    readonly details?: ApiErrorDetail[],
    readonly requestId?: string,
  ) {
    super(message);
    this.name = "ApiError";
  }
}

const FALLBACK_MESSAGE = "Falha inesperada ao falar com o servidor";

function isDetailList(value: unknown): value is ApiErrorDetail[] {
  return (
    Array.isArray(value) &&
    value.every(
      (item) =>
        typeof item === "object" &&
        item !== null &&
        typeof (item as ApiErrorDetail).path === "string" &&
        typeof (item as ApiErrorDetail).message === "string",
    )
  );
}

export function parseApiError(status: number, body: unknown): ApiError {
  const envelope =
    typeof body === "object" && body !== null ? (body as { error?: unknown }).error : undefined;

  if (typeof envelope !== "object" || envelope === null) {
    return new ApiError(status, "UNKNOWN", FALLBACK_MESSAGE);
  }

  const { code, message, details, requestId } = envelope as Record<string, unknown>;

  return new ApiError(
    status,
    typeof code === "string" ? code : "UNKNOWN",
    typeof message === "string" ? message : FALLBACK_MESSAGE,
    isDetailList(details) ? details : undefined,
    typeof requestId === "string" ? requestId : undefined,
  );
}

export function isApiError(value: unknown): value is ApiError {
  return value instanceof ApiError;
}
```

- [ ] **Passo 4: Rodar e ver passar**

Run: `npx vitest run src/lib/api-errors.test.ts`
Esperado: 5 testes passando.

- [ ] **Passo 5: Commit**

```bash
git add src/lib/api-errors.ts src/lib/api-errors.test.ts
git commit -m "feat(lib): adiciona ApiError e parse do formato de erro da API

Parse defensivo: 502 de proxy devolve HTML, não o envelope da API, e
isso não pode virar 'undefined' na tela. O requestId é preservado
porque é ele que liga o erro ao log do backend."
```

---

### Task 5: QueryClient com `staleTime` por rota (backlog 00.10)

**Arquivos:**
- Criar: `src/lib/query-client.ts`, `src/lib/query-client.test.ts`
- Modificar: `src/app/layout.tsx`, criar `src/app/providers.tsx`

**Interfaces:**
- Consome: `isApiError` da Task 4
- Produz:
  - `STALE_TIME` — objeto com `events`, `eventDetail`, `seats`, `reservations`, `tickets`, `catalog` (ms)
  - `createQueryClient(): QueryClient`
  - `<Providers>` — client component embrulhando `QueryClientProvider`

- [ ] **Passo 1: Instalar o TanStack Query**

```bash
npm i @tanstack/react-query
```

- [ ] **Passo 2: Escrever os testes que falham**

`src/lib/query-client.test.ts`:

```ts
import { describe, expect, it } from "vitest";

import { ApiError } from "./api-errors";
import { STALE_TIME, createQueryClient } from "./query-client";

describe("STALE_TIME", () => {
  it("mantém o mapa de assentos sempre fresco", () => {
    // Assento cacheado é o usuário clicando num lugar já vendido.
    expect(STALE_TIME.seats).toBe(0);
  });

  it("segue a tabela do 07-performance.md", () => {
    expect(STALE_TIME.events).toBe(60_000);
    expect(STALE_TIME.eventDetail).toBe(60_000);
    expect(STALE_TIME.reservations).toBe(0);
    expect(STALE_TIME.tickets).toBe(30_000);
    expect(STALE_TIME.catalog).toBe(300_000);
  });
});

describe("createQueryClient", () => {
  const retry = () => {
    const client = createQueryClient();
    return client.getDefaultOptions().queries?.retry as (
      failureCount: number,
      error: Error,
    ) => boolean;
  };

  it("não repete requisição que falhou por erro do cliente", () => {
    expect(retry()(0, new ApiError(403, "FORBIDDEN", "sem permissão"))).toBe(false);
    expect(retry()(0, new ApiError(404, "NOT_FOUND", "não achei"))).toBe(false);
  });

  it("repete erro de servidor até duas vezes", () => {
    const erro = new ApiError(500, "INTERNAL_ERROR", "falhou");
    expect(retry()(0, erro)).toBe(true);
    expect(retry()(1, erro)).toBe(true);
    expect(retry()(2, erro)).toBe(false);
  });

  it("repete erro de rede, que não é ApiError", () => {
    expect(retry()(0, new Error("Network Error"))).toBe(true);
  });
});
```

- [ ] **Passo 3: Rodar e ver falhar**

Run: `npx vitest run src/lib/query-client.test.ts`
Esperado: FALHA — não consegue resolver `./query-client`.

- [ ] **Passo 4: Implementar**

`src/lib/query-client.ts`:

```ts
import { QueryClient } from "@tanstack/react-query";

import { isApiError } from "./api-errors";

/** staleTime por rota, conforme a tabela do docs/doc-frontend/07-performance.md. */
export const STALE_TIME = {
  events: 60_000,
  eventDetail: 60_000,
  /** Zero de propósito: outra pessoa pode reservar entre o carregamento e o clique. */
  seats: 0,
  reservations: 0,
  tickets: 30_000,
  catalog: 300_000,
} as const;

const MAX_RETRIES = 2;

export function createQueryClient(): QueryClient {
  return new QueryClient({
    defaultOptions: {
      queries: {
        staleTime: STALE_TIME.events,
        refetchOnWindowFocus: true,
        retry: (failureCount: number, error: Error) => {
          // 4xx não melhora repetindo: entrada inválida, sem permissão ou inexistente.
          if (isApiError(error) && error.status < 500) return false;
          return failureCount < MAX_RETRIES;
        },
      },
      mutations: { retry: false },
    },
  });
}
```

- [ ] **Passo 5: Rodar e ver passar**

Run: `npx vitest run src/lib/query-client.test.ts`
Esperado: 5 testes passando.

- [ ] **Passo 6: Ligar o provider na aplicação**

`src/app/providers.tsx`:

```tsx
"use client";

import { QueryClientProvider } from "@tanstack/react-query";
import { useState, type ReactNode } from "react";

import { createQueryClient } from "@/lib/query-client";

export function Providers({ children }: { children: ReactNode }) {
  // useState garante um client por sessão de navegador, não um por render.
  const [client] = useState(createQueryClient);

  return <QueryClientProvider client={client}>{children}</QueryClientProvider>;
}
```

Em `src/app/layout.tsx`, embrulhe `{children}` com `<Providers>`.

- [ ] **Passo 7: Verificar**

```bash
npm run typecheck && npm run build
```
Esperado: ambos passam.

- [ ] **Passo 8: Commit**

```bash
git add -A
git commit -m "feat(lib): adiciona QueryClient com staleTime por rota

Retry não repete 4xx: entrada inválida ou falta de permissão não
melhora tentando de novo, só atrasa o feedback. O mapa de assentos
fica com staleTime 0 — cache ali é o usuário clicando em lugar
já vendido."
```

---

### Task 6: Mensagens em português e rótulos de campo (backlog 00.11)

**Arquivos:**
- Criar: `src/lib/messages.ts`, `src/lib/field-labels.ts`, `src/lib/field-labels.test.ts`

**Interfaces:**
- Consome: nada
- Produz:
  - `messages` — objeto por domínio (`network`, `auth`, `reservations`, `payment`, `checkin`)
  - `labelForPath(path: string): string`

`labelForPath` é consumido pelo `useApiFormErrors` no epic 02.

- [ ] **Passo 1: Escrever os testes que falham**

`src/lib/field-labels.test.ts`:

```ts
import { describe, expect, it } from "vitest";

import { labelForPath } from "./field-labels";

describe("labelForPath", () => {
  it("traduz um campo conhecido", () => {
    expect(labelForPath("email")).toBe("E-mail");
  });

  it("resolve notação de ponto pelo último segmento", () => {
    expect(labelForPath("event.price")).toBe("Preço");
  });

  it("traduz o corpo ausente para algo legível", () => {
    expect(labelForPath("(corpo)")).toBe("Formulário");
  });

  it("devolve o próprio path quando não conhece o campo", () => {
    expect(labelForPath("campoDesconhecido")).toBe("campoDesconhecido");
  });
});
```

- [ ] **Passo 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/field-labels.test.ts`
Esperado: FALHA — não consegue resolver `./field-labels`.

- [ ] **Passo 3: Implementar os rótulos**

`src/lib/field-labels.ts`:

```ts
/**
 * Os details[].message do VALIDATION_ERROR vêm do Zod, em inglês
 * ("Too small: expected string to have >=8 characters"). Traduzimos por campo,
 * não por tradução literal da frase — a frase muda quando o schema muda.
 */
const FIELD_LABELS: Record<string, string> = {
  name: "Nome",
  email: "E-mail",
  password: "Senha",
  title: "Título",
  description: "Descrição",
  price: "Preço",
  date: "Data",
  imageUrl: "Imagem",
  seatId: "Assento",
  code: "Código do ingresso",
  "(corpo)": "Formulário",
};

export function labelForPath(path: string): string {
  const direct = FIELD_LABELS[path];
  if (direct) return direct;

  const lastSegment = path.split(".").at(-1) ?? path;
  return FIELD_LABELS[lastSegment] ?? path;
}
```

- [ ] **Passo 4: Rodar e ver passar**

Run: `npx vitest run src/lib/field-labels.test.ts`
Esperado: 4 testes passando.

- [ ] **Passo 5: Criar as mensagens de fallback**

`src/lib/messages.ts`:

```ts
/**
 * Fallbacks por domínio. A API já devolve `message` em português na maioria
 * dos casos — prefira sempre exibir a dela. Isto cobre o que ela não cobre:
 * falha de rede, timeout e estados que só o cliente conhece.
 */
export const messages = {
  network: {
    offline: "Sem conexão. Verifique sua internet e tente de novo.",
    timeout: "O servidor demorou a responder. Tente de novo em instantes.",
    unexpected: "Algo deu errado. Se persistir, informe o código abaixo ao suporte.",
  },
  auth: {
    sessionExpired: "Sua sessão expirou. Entre novamente.",
    forbidden: "Você não tem permissão para acessar esta área.",
  },
  reservations: {
    seatTaken: "Este assento acabou de ser reservado por outra pessoa. Escolha outro.",
    expired: "O tempo da reserva acabou. Selecione o assento novamente.",
  },
  payment: {
    refused: "Pagamento recusado. Tente novamente ou escolha outra forma.",
  },
  checkin: {
    cameraDenied: "Não foi possível acessar a câmera. Use o código manual abaixo.",
  },
} as const;
```

- [ ] **Passo 6: Commit**

```bash
git add src/lib/messages.ts src/lib/field-labels.ts src/lib/field-labels.test.ts
git commit -m "feat(lib): adiciona mensagens pt-BR e rótulos de campo

Tradução do VALIDATION_ERROR por campo, não por frase: a mensagem do
Zod muda quando o schema muda, o nome do campo não."
```

---

### Task 7: shadcn/ui e a paleta (backlog 00.4)

**Arquivos:**
- Criar: `components.json`, `src/components/ui/*` (gerados por CLI)
- Modificar: `src/app/globals.css`

**Interfaces:**
- Consome: scaffold da Task 1
- Produz: tokens CSS (`--background`, `--foreground`, `--primary`, …) definidos nos três blocos de tema; componentes `Button`, `Input`, `Label`, `Dialog`, `DropdownMenu`, `Table`

- [ ] **Passo 1: Inicializar o shadcn/ui**

```bash
npx shadcn@latest init
```

Escolhas: estilo neutro, cor base **slate**, variáveis CSS **sim**.

- [ ] **Passo 2: Adicionar os componentes que a fundação e o kit vão usar**

```bash
npx shadcn@latest add button input label dialog dropdown-menu table skeleton
```

- [ ] **Passo 3: Estruturar os três blocos de tema**

Em `src/app/globals.css`, os tokens precisam existir em três lugares — essa é a estrutura que faz o modo `system` funcionar **sem** JavaScript:

```css
/* 1. Light é o padrão, definido no :root puro. */
:root {
  --background: oklch(0.99 0 0);
  --foreground: oklch(0.21 0.01 250);
  /* ... demais tokens do shadcn ... */
}

/* 2. Preferência do SO, valendo só quando não há escolha explícita de light. */
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) {
    --background: oklch(0.19 0.01 250);
    --foreground: oklch(0.96 0 0);
    /* ... */
  }
}

/* 3. Escolha explícita do usuário vence os dois anteriores. */
:root[data-theme="dark"] {
  --background: oklch(0.19 0.01 250);
  --foreground: oklch(0.96 0 0);
  /* ... */
}
```

Nenhum token pode ter sua **única** definição dentro de um `@media` ou de um `[data-theme]` — senão o modo oposto fica sem valor.

- [ ] **Passo 4: Ajustar a paleta para o espírito AdminLTE**

Restrições da spec, a conferir antes de seguir: sem gradiente roxo-azul; uma única cor de ação primária (azul sóbrio); superfícies com pouca saturação; bordas visíveis e discretas (`--border` com contraste real, não quase invisível).

- [ ] **Passo 5: Verificar contraste**

Confira `--foreground` sobre `--background` nos dois temas com um medidor de contraste. Esperado: **≥ 4.5:1**. Anote os valores; eles voltam no epic 06, nos quatro estados da portaria.

- [ ] **Passo 6: Verificar que compila**

```bash
npm run build
```

- [ ] **Passo 7: Commit**

```bash
git add -A
git commit -m "feat(ui): inicializa shadcn/ui com paleta sóbria de painel

Tokens definidos em três blocos (:root, prefers-color-scheme e
[data-theme]) para o modo 'system' funcionar sem JavaScript e sem
flash de tema errado."
```

---

### Task 8: Tema por cookie (backlog 00.5)

**Arquivos:**
- Criar: `src/lib/theme.ts`, `src/lib/theme.test.ts`, `src/app/theme-action.ts`, `src/components/layout/ThemeToggle.tsx`
- Modificar: `src/app/layout.tsx`

**Interfaces:**
- Consome: tokens da Task 7
- Produz:
  - `type ThemePreference = "light" | "dark" | "system"`
  - `THEME_COOKIE = "vz_theme"`
  - `parseTheme(value: string | undefined): ThemePreference`
  - `themeAttribute(theme: ThemePreference): "light" | "dark" | undefined`
  - Server Action `setTheme(theme: ThemePreference): Promise<void>`

- [ ] **Passo 1: Escrever os testes que falham**

`src/lib/theme.test.ts`:

```ts
import { describe, expect, it } from "vitest";

import { parseTheme, themeAttribute } from "./theme";

describe("parseTheme", () => {
  it("aceita os três valores válidos", () => {
    expect(parseTheme("light")).toBe("light");
    expect(parseTheme("dark")).toBe("dark");
    expect(parseTheme("system")).toBe("system");
  });

  it("cai em system quando o cookie não existe", () => {
    expect(parseTheme(undefined)).toBe("system");
  });

  it("cai em system quando o cookie foi adulterado", () => {
    expect(parseTheme("<script>")).toBe("system");
  });
});

describe("themeAttribute", () => {
  it("devolve o valor do atributo para escolha explícita", () => {
    expect(themeAttribute("light")).toBe("light");
    expect(themeAttribute("dark")).toBe("dark");
  });

  it("não devolve atributo em system, para o CSS decidir pela mídia", () => {
    expect(themeAttribute("system")).toBeUndefined();
  });
});
```

- [ ] **Passo 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/theme.test.ts`
Esperado: FALHA — não consegue resolver `./theme`.

- [ ] **Passo 3: Implementar**

`src/lib/theme.ts`:

```ts
export type ThemePreference = "light" | "dark" | "system";

export const THEME_COOKIE = "vz_theme";
export const THEME_MAX_AGE = 60 * 60 * 24 * 365;

const VALID: readonly ThemePreference[] = ["light", "dark", "system"];

export function parseTheme(value: string | undefined): ThemePreference {
  return VALID.includes(value as ThemePreference) ? (value as ThemePreference) : "system";
}

/**
 * Em "system" não devolvemos atributo: sem ele, o bloco
 * @media (prefers-color-scheme: dark) do globals.css assume.
 */
export function themeAttribute(theme: ThemePreference): "light" | "dark" | undefined {
  return theme === "system" ? undefined : theme;
}
```

- [ ] **Passo 4: Rodar e ver passar**

Run: `npx vitest run src/lib/theme.test.ts`
Esperado: 5 testes passando.

- [ ] **Passo 5: Criar a Server Action**

`src/app/theme-action.ts`:

```ts
"use server";

import { cookies } from "next/headers";

import { THEME_COOKIE, THEME_MAX_AGE, type ThemePreference } from "@/lib/theme";

export async function setTheme(theme: ThemePreference): Promise<void> {
  const store = await cookies();

  store.set(THEME_COOKIE, theme, {
    path: "/",
    maxAge: THEME_MAX_AGE,
    sameSite: "lax",
    // Sem httpOnly: preferência de UI não é credencial.
  });
}
```

- [ ] **Passo 6: Aplicar o tema no layout raiz**

Em `src/app/layout.tsx`, torne o componente `async`, leia o cookie e aplique o atributo:

```tsx
import { cookies } from "next/headers";

import { parseTheme, THEME_COOKIE, themeAttribute } from "@/lib/theme";

export default async function RootLayout({ children }: { children: React.ReactNode }) {
  const theme = parseTheme((await cookies()).get(THEME_COOKIE)?.value);

  return (
    <html lang="pt-BR" data-theme={themeAttribute(theme)} suppressHydrationWarning>
      <body>{/* ... Providers, children ... */}</body>
    </html>
  );
}
```

Ler cookie torna a rota dinâmica. É o custo consciente de não ter flash de tema errado — anote isso no `DECISIONS.md` no epic 08.

- [ ] **Passo 7: Criar o alternador**

`src/components/layout/ThemeToggle.tsx`:

```tsx
"use client";

import { Monitor, Moon, Sun } from "lucide-react";
import { useRouter } from "next/navigation";
import { useTransition } from "react";

import { setTheme } from "@/app/theme-action";
import { Button } from "@/components/ui/button";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import type { ThemePreference } from "@/lib/theme";

const OPTIONS: { value: ThemePreference; label: string; Icon: typeof Sun }[] = [
  { value: "light", label: "Claro", Icon: Sun },
  { value: "dark", label: "Escuro", Icon: Moon },
  { value: "system", label: "Sistema", Icon: Monitor },
];

export function ThemeToggle({ current }: { current: ThemePreference }) {
  const router = useRouter();
  const [pending, startTransition] = useTransition();

  function choose(theme: ThemePreference) {
    startTransition(async () => {
      await setTheme(theme);
      // O atributo data-theme é renderizado no servidor; refresh o repinta.
      router.refresh();
    });
  }

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="ghost" size="icon" disabled={pending} aria-label="Alternar tema">
          <Sun className="h-4 w-4 dark:hidden" />
          <Moon className="hidden h-4 w-4 dark:block" />
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        {OPTIONS.map(({ value, label, Icon }) => (
          <DropdownMenuItem
            key={value}
            onSelect={() => choose(value)}
            aria-current={current === value}
          >
            <Icon className="mr-2 h-4 w-4" />
            {label}
          </DropdownMenuItem>
        ))}
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

Ícones do `lucide-react`, **nunca** emoji. O `current` vem do layout, que já leu o cookie.

- [ ] **Passo 8: Verificar manualmente**

Run: `npm run dev`

Esperado, na ordem:
1. Alternar para escuro repinta a página.
2. Recarregar (F5) mantém o tema, **sem** piscar claro antes.
3. Escolher "sistema" e trocar o tema do SO acompanha, sem recarregar.
4. `document.cookie` no console mostra `vz_theme` — e apenas ele.

- [ ] **Passo 9: Commit**

```bash
git add -A
git commit -m "feat(ui): implementa tema claro/escuro persistido em cookie

Cookie em vez de localStorage: o servidor lê a preferência e já
renderiza o atributo no <html>, então não há flash de tema errado nem
mismatch de hidratação — e nem script bloqueante no <head>."
```

---

### Task 9: `AppShell` (backlog 00.6)

**Arquivos:**
- Criar: `src/components/layout/AppShell.tsx`, `Sidebar.tsx`, `Topbar.tsx`, `src/stores/ui-store.ts`
- Modificar: `src/app/layout.tsx`, `src/app/page.tsx`

**Interfaces:**
- Consome: `ThemeToggle` da Task 8
- Produz: `<AppShell>{children}</AppShell>`; `useUiStore` com `sidebarCollapsed` e `toggleSidebar()`

- [ ] **Passo 1: Instalar o Zustand**

```bash
npm i zustand
```

- [ ] **Passo 2: Criar o store de UI**

`src/stores/ui-store.ts`:

```ts
import { create } from "zustand";

interface UiState {
  sidebarCollapsed: boolean;
  toggleSidebar: () => void;
}

export const useUiStore = create<UiState>((set) => ({
  sidebarCollapsed: false,
  toggleSidebar: () => set((state) => ({ sidebarCollapsed: !state.sidebarCollapsed })),
}));
```

- [ ] **Passo 3: Construir a casca**

`src/components/layout/AppShell.tsx`:

```tsx
import type { ReactNode } from "react";

import { Sidebar } from "./Sidebar";
import { Topbar } from "./Topbar";
import type { ThemePreference } from "@/lib/theme";

interface AppShellProps {
  children: ReactNode;
  title: string;
  theme: ThemePreference;
}

export function AppShell({ children, title, theme }: AppShellProps) {
  return (
    <div className="flex h-dvh overflow-hidden bg-background text-foreground">
      <Sidebar />
      <div className="flex min-w-0 flex-1 flex-col">
        <Topbar title={title} theme={theme} />
        <main className="min-h-0 flex-1 overflow-y-auto p-4">{children}</main>
      </div>
    </div>
  );
}
```

`src/components/layout/Topbar.tsx`:

```tsx
"use client";

import { PanelLeft } from "lucide-react";

import { ThemeToggle } from "./ThemeToggle";
import { Button } from "@/components/ui/button";
import type { ThemePreference } from "@/lib/theme";
import { useUiStore } from "@/stores/ui-store";

export function Topbar({ title, theme }: { title: string; theme: ThemePreference }) {
  const toggleSidebar = useUiStore((state) => state.toggleSidebar);

  return (
    <header className="flex h-12 shrink-0 items-center gap-3 border-b border-border px-4">
      <Button variant="ghost" size="icon" onClick={toggleSidebar} aria-label="Recolher menu">
        <PanelLeft className="h-4 w-4" />
      </Button>
      <h1 className="truncate text-sm font-semibold tracking-tight">{title}</h1>
      <div className="ml-auto">
        <ThemeToggle current={theme} />
      </div>
    </header>
  );
}
```

`src/components/layout/Sidebar.tsx`: client component lendo `sidebarCollapsed` do `useUiStore`, com largura `w-56` expandida e `w-14` recolhida, transição só na largura. Nesta fundação a navegação é um placeholder — os itens reais dependem do papel do usuário e chegam no epic 01.

Densidade de painel operacional, não de landing page: a topbar tem 48 px de altura (`h-12`), não 80; paddings contidos; tipografia com hierarquia real (peso **e** tamanho variando, não só cor); bordas discretas separando as regiões. Nada de sombra flutuante.

- [ ] **Passo 4: Usar a casca na página inicial**

Substitua o conteúdo padrão do `create-next-app` em `src/app/page.tsx`. O boilerplate do Next não pode sobreviver ao epic.

```tsx
import { cookies } from "next/headers";

import { AppShell } from "@/components/layout/AppShell";
import { parseTheme, THEME_COOKIE } from "@/lib/theme";

export default async function HomePage() {
  const theme = parseTheme((await cookies()).get(THEME_COOKIE)?.value);

  return (
    <AppShell title="Início" theme={theme}>
      <p className="text-sm text-muted-foreground">
        Fundação do projeto. As telas chegam a partir do epic 02.
      </p>
    </AppShell>
  );
}
```

A página lê o cookie e repassa ao `AppShell` porque o título muda a cada rota — o `layout.tsx` cuida do atributo no `<html>`, a página cuida da sua própria casca. No epic 01, quando surgirem os grupos de rota por papel, esse `cookies()` sobe para o layout de cada grupo em vez de se repetir por página.

- [ ] **Passo 5: Verificar**

```bash
npm run lint && npm run typecheck && npm run test && npm run build
```

Esperado: os quatro passam. Abra em 1280 px e em 375 px: a sidebar recolhe no estreito e nada estoura horizontalmente.

- [ ] **Passo 6: Commit**

```bash
git add -A
git commit -m "feat(ui): adiciona AppShell com sidebar e topbar

Densidade de painel operacional em vez de espaçamento de landing page:
a tela do organizador e a da portaria são ferramentas de trabalho."
```

---

### Task 10: CI no GitHub Actions (backlog 00.7)

**Arquivos:**
- Criar: `.github/workflows/ci.yml`

**Interfaces:**
- Consome: scripts das Tasks 2 e 3
- Produz: check obrigatório no PR

- [ ] **Passo 1: Escrever o workflow**

`.github/workflows/ci.yml`:

```yaml
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run lint
      - run: npm run format:check
      - run: npm run typecheck
      - run: npm run test
      - run: npm run build
```

Os passos são separados de propósito: o log do GitHub mostra **qual** verificação quebrou sem ninguém precisar abrir a saída inteira.

- [ ] **Passo 2: Verificar localmente antes de empurrar**

```bash
npm run lint && npm run format:check && npm run typecheck && npm run test && npm run build
```

Esperado: os cinco passam. Se `format:check` falhar, rode `npm run format` e recommite.

- [ ] **Passo 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: verifica lint, formatação, tipos, testes e build no PR

Passos separados para o log dizer qual verificação quebrou sem
precisar abrir a saída inteira."
```

---

### Task 11: Ambiente e instruções de execução (backlog 00.8)

**Arquivos:**
- Criar: `.env.example`, `README.md`

**Interfaces:**
- Consome: tudo acima
- Produz: `README.md` com a seção "como rodar" — o README completo (badges, decisões, testes) é o epic 08

- [ ] **Passo 1: Criar o `.env.example`**

```bash
# URL da API. Server-only de propósito: sem NEXT_PUBLIC_, não entra no bundle.
API_URL=http://localhost:3000

# true em produção
SESSION_COOKIE_SECURE=false
```

- [ ] **Passo 2: Confirmar que `.env` está ignorado**

Run: `git check-ignore -v .env`
Esperado: imprime a linha do `.gitignore` que o cobre. Se não imprimir nada, o `.env` seria commitado — corrija antes de seguir.

- [ ] **Passo 3: Escrever o README mínimo**

Seções, nesta ordem: o que é o projeto (dois parágrafos), pré-requisitos (Node 22, o backend rodando), como rodar (`npm ci`, copiar `.env.example`, `npm run dev`, abrir `:3001`), scripts disponíveis (tabela), e um aviso de que o backend ocupa a `:3000` e por isso o front usa a `:3001`.

Deixe um marcador visível de que badges, decisões e estratégia de testes chegam no epic 08 — para ninguém achar que o README ficou pela metade por descuido.

- [ ] **Passo 4: Verificar seguindo as próprias instruções**

Em um clone limpo (`git clone . /tmp/verzel-check`), siga o README ao pé da letra. Se faltar algum passo, o README está errado — corrija o README, não improvise.

- [ ] **Passo 5: Commit**

```bash
git add .env.example README.md
git commit -m "docs: adiciona .env.example e instruções de execução

API_URL sem prefixo NEXT_PUBLIC_ para a URL da API não vazar no
bundle do cliente."
```

---

## Fechamento do epic

- [ ] **Verificação final**

```bash
npm run lint && npm run format:check && npm run typecheck && npm run test && npm run build
```

Esperado: os cinco passam. Cole a saída no PR — afirmação sem evidência não conta.

- [ ] **Marcar as tasks 00.1 a 00.11 no `docs/BACKLOG.md`** e atualizar o contador da tabela para `✅ 11/11`.

- [ ] **Abrir o PR**

```bash
git push -u origin feat/00-fundacao
gh pr create --base main --title "feat: fundação do projeto (epic 00)" --body "..."
```

O corpo do PR liga ao spec 000 e à faixa 00.1–00.11 do backlog, e traz a saída da verificação final.

- [ ] **Critério de pronto:** o CI está verde, a casca abre nos dois temas sem flash, nenhum token ou URL de API aparece no bundle do cliente, e `docs/BACKLOG.md` reflete o que foi entregue.
